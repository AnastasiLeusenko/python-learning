# Python — Шпаргалка (Anastasiia)

## Термінал / Git
- Перейти в папку проєкту: `cd ~/python-learning`
- Відкрити VS Code з поточної папки: `code .`
- Git цикл: `git add .` → `git commit -m "опис"` → `git push`
- Зупинити нескінченний цикл / завислу програму: `Ctrl + C`

## Типи даних
- `int` — ціле число, `float` — дробове, `str` — текст (у лапках), `bool` — True/False
- Python НЕ робить неявних перетворень типів: `5 + "5"` → помилка (на відміну від JS)
- `str(x)` — перетворити на текст, `int(x)` / `float(x)` — перетворити на число
- `bool()`: порожнє → False, непорожнє → True

## Змінні
- `=` означає "присвоїти", а не "дорівнює"
- Python регістрозалежний: `name` ≠ `Name`
- Обмін значень: завжди через `temp`, рятуємо те, що загубиться першим

## Умови (if/elif/else)
- Відступи замість `{}` (стандарт — 4 пробіли)
- `and`, `or`, `not` — логічні оператори
- Тернарний вираз: `результат_true if умова else результат_false`
- Ланцюжок порівнянь: `12 <= hour < 18`

## Цикли (for/while)
- `range(5)` → 0,1,2,3,4 (останнє число НЕ включається)
- `range(1, 6)` → 1,2,3,4,5
- `while` завжди потребує зміни умови всередині, інакше — нескінченний цикл

## Часті помилки
- `SyntaxError` — неправильна граматика коду (Python не може навіть почати виконання)
- `NameError` — звертання до неіснуючої змінної
- `TypeError` — операція між несумісними типами (напр. `int + str`)
- `IndentationError` — неправильний відступ